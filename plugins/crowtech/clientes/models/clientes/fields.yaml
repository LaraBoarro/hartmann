fields:
    logocliente:
        label: 'Upload de arquivo'
        mode: image
        useCaption: true
        thumbOptions:
            mode: crop
            extension: auto
        span: auto
        type: fileupload
